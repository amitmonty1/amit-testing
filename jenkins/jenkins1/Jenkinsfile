#!groovy
@Library('amitdude') _

pipeline{
    agent any
    stages{
        stage("dude"){
            steps{
                deleteDir()
                
                checkout scm
                holyCrap()
                echo "dude 12"
            }
        }
    }
}