#!groovy
@Library('amitdude') _

pipeline{
    agent any
    stages{
        stage("dude"){
            steps{
                deleteDir()
                
                checkout scm
                myScript()
                echo "dude 12"
            }
        }
    }
}